package com.jtouzy.fastrecord.statements.context;

/**
 * SQL update (U,D) condition chain (in WHERE clause).
 */
public interface UpdateConditionChain extends ConditionChain<UpdateConditionChain> {
}
